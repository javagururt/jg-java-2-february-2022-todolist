spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    password: sa
    username: sa
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  liquibase:
    change-log: liquibase-changeLog.xml