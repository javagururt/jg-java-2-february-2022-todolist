Создайте в классе ApplicationContext следующее свойство:

    private Map<Class, Object> beans = new HashMap<>();

Идея этого мапа состоит в том, чтобы хранить маппинг
класса на его инстанцию. В дальнейшем этот мап позволит
получать инстанцию объекта указывая только его класс.